# Core — inference, CLI, evaluation (CPU-safe)
pydantic>=2.5.0
pyyaml>=6.0
httpx>=0.26.0
rich>=13.7.0
typer>=0.9.0
numpy>=1.26.0

# Fine-tuning — required for sleep-cycle QLoRA training (GPU only)
# Install on Colab A100/T4 with:
#   pip install "unsloth[colab-new]" trl transformers datasets
# Omit on CPU-only machines — finetuner.py degrades gracefully without them.
# unsloth[colab-new]
# trl>=0.8.0
# transformers>=4.40.0
# datasets>=2.19.0
